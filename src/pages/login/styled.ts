import styled from 'styled-components';

export const LoginWrapper = styled.div`
    overflow: hidden;
    
    .loginForm {
        width: 600px;
        margin: 200px auto;

        .loginTitle {
            text-align: center;
            margin: 20px 0;
        }
    }
`;